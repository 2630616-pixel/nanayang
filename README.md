# 간단한 투두앱

로컬 브라우저에서 동작하는 간단한 투두 앱입니다. 데이터는 `localStorage`에 저장됩니다.

사용 방법

- 개발 서버(간단한 정적 서버)를 실행하려면:

```bash
python3 -m http.server 8000
```

그리고 브라우저에서 `http://localhost:8000/index.html`을 열어보세요.

혹은 파일을 직접 열어도 동작합니다(브라우저의 보안 설정에 따라 localStorage 접근이 제한될 수 있음).
# nanayang